# 책 근거 대조 — 한국공인회계사회 『K-IFRS 실무사례와 해설 11 복합금융상품』

앞서 부속예제 18종을 대조해 `책예제_대조표.md` 를 만들었다. 그 예제들이 실린
**원전**이 이 책이고, 여기서는 261쪽 본문과 `cb_app.py` 를 맞춰본 기록을 남긴다.

원본 · 구글 드라이브 파일 `1AZeivpdTD0oBiG74i2czK6W1fSncvkut`
(4.3MB, 저작권 문제로 저장소에 넣지 않는다)

---

## 1. 근거가 확인된 것

| `cb_app.py` 의 처리 | 근거 |
|---|---|
| 매도청구권을 파생상품**자산**으로 차감 | 기준서 1109 문단 4.3.1 · 회계기준원 질의회신 2022-I-KQA006 · 금융위 2022.5.3 감독지침 (책 p142) |
| 부채요소를 먼저 정하고 자본은 잔여 | 1032 문단 31·32 · 사례 1108 (57 − 콜 2 = 55, 자본 5) |
| 발행가액을 전환사채와 콜옵션에 배분 | 사례 1105 해설2 (발행 1,000 = CB 1,200 − 콜 200) |
| 위험중립확률을 **연속복리 선도이자율**로 산출 | 3.7.4.3 |
| 현물이자율 = −ln(DF)/t 로 연속화 | 사례 1130 (zero rate continuous 열) |
| 부트스트래핑 순차 대입 | 사례 1130 · 부속예제 5-12 (소수점 5자리 일치) |
| 후방귀납 구조 | 3.7.5.1 |
| TF = 채권·지분 분해 | 4.2.1 (cash-or-nothing 풋 + asset-or-nothing 콜) |

---

## 2. 고친 것

### 2.1 매도청구권 — 옵션차익혼합할인법을 더했다

**문제.** 격자 안에서 `MIN(계속보유, 콜금액)` 으로 처리하고 있었다. 이건
**회사가 직접 되사가는** 발행자 콜옵션 방식이다. 그런데 티사이언티픽 공시는
*"발행회사 **및 발행회사가 지정하는 자**"* — 제3자 지정 가능 콜옵션이다.

책이 둘을 명확히 가른다 (4.2~4.3).

| | 성격 | 계산 |
|---|---|---|
| 발행자 콜옵션 | 채권 부분. 조기상환 페이오프 | 격자 안에서 처리 |
| **제3자 지정 가능 콜옵션** | **복합옵션** (a call on a convertible bond) | 전환사채를 기초자산으로 별도 평가 |

> 제3자 지정 가능 콜옵션은 … 그 기초자산이 전환사채인 '옵션에 대한 옵션(an
> option on an option)'의 특성을 반영하는 **'복합옵션(compound option)' 평가
> 모형이 적용되어야 한다** — 사례 1105 해설1

책이 드는 세 방법 (4.1.1)

| 방법 | 책의 평가 |
|---|---|
| 상품자체기초자산적용법 | 전환사채 변동성 관측이 어려워 부적절 |
| 유무가치비교법 | 종전 앱이 쓰던 것 |
| **옵션차익혼합할인법** | *"실무적으로 많이 채택"*, 책이 이 기준으로 설명 |

> 유무가치비교법과 옵션차익혼합할인법은 개념적으로 그 결과가 동일하여야 하나
> 세부적인 구현방법에서 시장에서의 실무가 다양하게 진행되고 있어 그 차이가
> 종종 발생하는 것으로 보여진다.

**넣은 것.** `call_third_party()` 를 새로 만들고 `Terms.k_method` 로 고르게 했다.
기본값 0(유무가치비교법)이라 **기존 결과와 예전 시나리오 JSON 이 그대로 유지된다.**

새 격자를 만들지 않는다. `engine()` 이 노드마다 `E · B · V · P` 를 담아 돌려주므로
그 `memo` 를 한 번 더 역진하면 된다. 자식 노드 키(`up` · `dn`)를 노드에 남기게 해
리픽싱 경로를 다시 계산할 필요도 없앴다.

**기초자산 정의에 주의한다.**

> 콜옵션의 기초자산은 **콜과 그 부속조항**(콜행사기간동안 투자자에게 부가된
> **채권보유의무** 등)**을 포함하지 않은** 전환사채이다 — 4.4.2 · 4.4.3

`decompose()` 의 `full = engine(tm, call=False)` 이 정확히 이것이다 — 콜도 없고
`k_lock`(의무보유)도 걸리지 않는다. 부속예제 4-4의 기초자산 트리도
`Flag(Conversion)` 에 콜 조건이 없고 `Flag(Call)=0` 인 것을 확인했다.

반대로 유무가치비교법에서는 의무보유가 콜 값에 포함되는 것이 맞다 (4.5.2 는
부속조항을 빼라고 하지 않는다). 두 방법을 나란히 둘 때 이 차이를 지켜야 한다.

**부속예제 4-4 재현** (잔존기간을 책과 같이 정확히 5년으로 맞춤)

| | 앱 | 책 | 차이 |
|---|---|---|---|
| 기초자산 CB (콜 없음) | 130.6027 | 130.6017 | +0.0010 |
| 방법 1 혼합할인율 | **29.7113** | 29.7102 | +0.0011 |
| 방법 2 지분·부채 분리 | **29.7976** | 29.7966 | +0.0010 |
| 유무가치비교법 | 31.9657 | 31.9647 | +0.0010 |

사례 4-1을 재현했을 때와 같은 잔차다.

**티사이언티픽에 적용하면**

| 방법 | 매도청구권 | 전환권대가 |
|---|---|---|
| 유무가치비교법 | 13.5173 | 40.3336 |
| 혼합할인율 | 8.9875 | 35.8038 |
| 지분·부채 분리 | 9.1899 | 36.0062 |

**수식 조서에도 넣었다.** ⑰ 구성비율 · ⑱ 혼합할인율 · ⑲ 콜 페이오프 · ⑳ 매도청구권가치
네 시트를 더해 옵션차익혼합할인법을 수식으로 편다. 결과 시트에 두 방법이 나란히 나오고
가정 시트의 `매도청구권 평가방법` 값(0 유무가치 / 1 혼합할인율)으로 어느 쪽을 적용할지
고른다. 네 시나리오에서 엔진과 소수점까지 일치하는 것을 확인했다 (티사이언티픽 25.6848
등). 방법 2(지분·부채 분리)는 아직 값 조서에만 있다.

그 과정에서 **수식 조서의 70% 트랜치가 콜을 켠 채 계산되고 있던 것**도 찾아 고쳤다.
`08 금융상품가치` 를 비롯한 주 트리가 매도청구 플래그를 켜고 있어 70% 트랜치가 콜 없는
전환사채가 아니었다. 매도청구권자산이 1% 과소 산출됐다. 주 트리(05·06·07·08·09·13·14)의
콜 플래그를 끄고 콜은 `15 30% 트랜치` 에서만 반영하도록 했다.

**남은 판단.** 책은 접근법 1(제3자 이연지정관점)과 2(제3자 가상지정관점) 중
하나를 고르지 않는다. FAQ(p149)에서 *"활성시장이 없어 역사적 변동성을 쓰는
상황이고, 이 변동성이 접근법 1과 2 중 어느 방법에 더 부합하는 값인지 구분할 수
없다"* 고 인정한다.

### 2.2 전환권이 부채일 때 배분이 틀렸다

`allocate()` 의 `liability` 갈래가 이랬다.

```python
deriv = b2 - b0 - ca          # ← "- ca" 가 잘못
host_acc = 100 - deriv + ca
```

매도청구권은 제3자 지정이 가능해 기준서 1109 문단 4.3.1 상 **별도의 금융상품**
이다. 복합내재파생상품에 들어가지 않으므로 거기서 빼면 안 된다. 사례 1105
해설2대로 발행가액을 배분하면 **전환사채에 배분된 금액은 100 + ca** 다.

**합칠 것을 합치는 것 자체는 이미 맞았다.**

> 전환권 및 풋옵션(또는 콜옵션)은 서로 독립적이지 아니하므로 하나의
> 복합내재파생상품으로 처리하여야 하며 … **'전체로서(as a whole)'** 주계약과
> 분리하여 … 파생상품부채라는 **하나의 회계단위**로 인식 및 측정하고 표시하는
> 것이 적절하다 — 책 p51, 기준서 1109 문단 B4.3.4

| 티사이언티픽 · 전환권 부채 | 종전 | 고친 뒤 |
|---|---|---|
| 주계약 (잔여) | 44.4977 | **30.9804** |
| 복합내재파생상품 · 파생상품부채 | 69.0196 | **82.5369** |
| 매도청구권 · 파생상품자산 | −13.5173 | −13.5173 |
| 합계 | 100.0000 | 100.0000 |

**13.5173 씩** 어긋나 있었다.

조서의 **분개표와 수식 조서에도 같은 옛 산식이 남아 있었다.** 배분표는 `allocate()` 를
쓰는데 분개표는 따로 계산해 같은 시트 안에서 두 표가 달랐다. 분개를 배분표에서 뒤집어
만들도록 바꿔 다시 갈라질 수 없게 했다. 수식 조서의 결과 시트도 `C6−C13−C16` 에서
`C6−C13` 으로 고쳤다.

`equity` 갈래는 그대로 뒀다. 전환권이 자본이면 남는 내재파생이 조기상환권
하나뿐이라 합칠 대상이 없고, 계산이 1032 문단 31·32 및 사례 1108과 맞는다.

### 2.3 BDT 적용 필요여부 판정을 검산 탭에 넣었다

BDT 모형 자체는 만들지 않는다. **써야 하는지 판단할 재료**만 보여준다.
전부 기존 엔진을 다시 돌려 얻는 값이다.

| 지표 | 계산 |
|---|---|
| 금리 민감도 | 무위험 곡선 ±1%p 평행이동 시 평가액 변화 |
| 주가 민감도 | 변동성 ±10%p 시 평가액 변화 |
| 비율 | 금리 ÷ 주가 |
| 스프레드 비중 | (Rd − Rf) ÷ Rd, 잔존만기 시점 |

| | 금리 ±1%p | 변동성 ±10%p | 비율 | 스프레드 비중 |
|---|---|---|---|---|
| 티사이언티픽 | 0.06% | 3.79% | 0.017 | 87% |
| 우량 가정 | 0.47% | 5.33% | 0.088 | 36% |

비율이 0.2 미만이거나 스프레드 비중이 70%를 넘으면 *"금리를 확률변수로 둘 실익이
작다"*, 아니면 *"BDT 등 금리모형 검토 필요"* 로 판정한다.

근거로 부속예제 4-3 실측을 함께 적었다 — 금리변동성 20%를 넣어도 조기상환권이
832.14 에서 836.22 로 **0.49%** 만 움직인다.

### 2.4 평가기법 문서화

책 4.6이 요구한다.

> 별도의 타당한 사유가 있지 않다면 기존 평가기법의 유지를 권장한다 — 4.6.1
>
> 선택한 평가기법이 해당 상품 평가에 적합하다는 판단 근거 등을 제시하고 관련
> 내용을 문서화할 필요가 있다 — 4.6.2

- 조서 가정 시트에 `매도청구권 평가방법` 행을 더했다
- 회계처리 시트 인용을 1032 문단 31 하나에서 넷으로 늘렸다 —
  1032 문단 31·32, 1109 문단 4.3.1, 질의회신 2022-I-KQA006, 1109 문단 B4.3.4

---

## 3. 뒤이어 반영한 것

> **갱신** — 3.1 과 3.2 는 이후 반영했다. 아래는 그 배경과 근거다.

### 3.1 현물이자율 입력의 복리 관행

책 3.7.4.4가 **오류 사례**로 명시한다.

> 회사채 제로커브 생성시 분기복리 기준의 현물이자율을 생성한 후, 할인계수나
> 위험중립확률 산출 시 연속복리 기준으로 계산하는 것은 오류이다

`spot_from_zero()` 는 `math.log(1+r)` 로 **연복리를 가정해 고정**한다. 사례 1130의
zero rate 가 연복리이므로 그 출처에는 맞지만, 분기복리 곡선을 받으면 틀린다.

| 5년 현물 19.05% | 연속환산 | DF | 주계약 |
|---|---|---|---|
| 연복리 (현재) | 17.4373% | 0.41817 | 41.82 |
| 분기복리 | 18.6103% | 0.39435 | 39.44 |

**반영했다.** `spot_from_zero(pts, m)` 가 `m·ln(1 + r/m)` 로 환산하고, `curves()` 가
`cmp_rf` · `cmp_cr` 를 넘긴다. `m=1` 이면 종전 `ln(1+r)` 로 되돌아와 기존 결과가 바뀌지
않는다. 사이드바에 `입력 유형` 선택기를 만들어 `spot` 모드를 화면에서 쓸 수 있게 했고,
고를 때 이표 횟수를 복리 횟수로 맞추라는 안내를 띄운다. 조서 가정 시트에도 복리 횟수를
함께 적는다.

### 3.2 조기상환권 밀접관련성 판단

사례 1105 해설3이 판단 논리를 준다.

> 주계약의 상각후원가는 전환사채에 배분된 거래가격인 CU1,200을 기준으로
> 산출되나, 풋옵션 행사금액은 액면금액인 CU1,000을 기준으로 산출되므로 … 거의
> 같지 않을 것이다. 따라서 풋옵션은 주계약과 분리하여 회계처리하는 것이 적절하다

**반영했다.** 검산 탭에 첫 조기상환일의 행사금액과 같은 시점 주계약 상각후원가를
나란히 띄우고 차이가 10%를 넘으면 분리, 아니면 문단 B4.3.5(5)(가) 예외 가능성을
알린다. 배분 자체는 여전히 분리를 전제하므로, 예외에 해당하면 회계처리 탭 값을 그대로
쓰기 전에 판단을 확인하라는 문구를 함께 띄운다.

티사이언티픽은 행사금액 100.00 대 상각후원가 55.52 로 **80.1%** 차이가 나 분리가 맞다.

## 3.3 전면 재검토에서 찾은 것

**만기 이자가 빠져 있었다.** 표면이자가 있는 사채에서 만기 노드가 이자를 더하지 않고
상환금액만 잡았다. 책 5-7 의 만기 현금흐름은 액면이자 50 + 원금 10,000 + 상환할증금
1,653.14 다. 표면 3%/보장 7% 로 시험하니 주계약이 55.9719 로, 확정현금흐름 현가
56.2533 보다 0.2814 낮았다. 만기 노드의 현금 갈래에 이자를 더했고 수식 조서의 만기
열도 시트 성격에 맞춰 맞췄다. 표면이자율이 0 이면 값이 변하지 않는다.


경계 조건 14가지를 훑다 매도청구권 계산의 결함 둘을 찾았다.

**의무보유가 전환을 앞당기고 있었다.** 30% 트랜치의 전환 시작을 `k_lock` 으로 **덮어쓰고**
있었다. 의무보유는 전환을 **늦추기만** 해야 하는데, `k_lock` 이 전환 시작보다 이르면
오히려 30% 트랜치가 먼저 전환할 수 있게 됐다. 그러면 콜을 붙인 쪽이 더 비싸져
**매도청구권이 음수**가 된다. 전환 시작이 200개월인 계약으로 시험하니 −0.5449 가 나왔다.
`max(전환 시작, 의무보유)` 로 고쳤다. 수식 조서의 `30% 전환 시작` 스텝도 같이 맞췄다.

**매도청구 기간이 격자 밖인데 값이 남았다.** 행사 가능한 시점이 하나도 없으면
유무가치비교법의 차액에 **의무보유 효과만** 남아 1.2300 이 잡혔다. 옵션차익혼합할인법은
같은 조건에서 0 을 내 두 방법이 어긋났다. 행사 가능 시점이 없으면 매도청구권을 0 으로
둔다.

기본 설정의 값은 세 방법 모두 그대로다 (13.5173 / 8.9875 / 9.1899).

## 3.4 값 조서와 수식 조서를 맞췄다

수식 조서를 `formulas` 로 실제 계산해 엔진과 대조하니 **네 가지가 어긋나 있었다.**
그동안 수식 조서는 구조만 보고 숫자를 맞춰본 적이 없었다.

**할인에 한 스텝 뒤 이자율을 썼다.** 트리 수식이 `{다음 열}$11·$12·$16·$17` 을 참조했다.
열 i 의 11·12행은 구간 [i, i+1] 의 선도이자율이므로 i+1 에서 i 로 할인할 때는 **열 i** 를
써야 한다. 게다가 마지막 열에는 이자율이 비어 있어 **마지막 한 스텝이 아예 할인되지
않았다.** 주계약이 40.21 로, 옳은 값 37.52 보다 높게 나왔다. 27곳을 고쳤다.

**⑫ GS 할인율·⑱ 혼합할인율의 기준 시점.** 이 두 시트는 자식을 직전 시점으로 할인할 때
쓰이므로 **직전 구간**의 선도이자율을 써야 한다.

**조기상환·매도청구 행사금액이 옛 산식이었다.** 상환할증금을 고칠 때 엔진만 바꾸고
수식 조서를 놓쳤다. 표면 3%·보장 7%·반기·5년에서 조기상환금액이 141.06 대 123.46 으로
17.6 벌어졌다. 세 곳(트리 헤더 2행, ⑯ 부채요소)을 맞췄다.

**GS 전환확률의 판단 기준.** 엔진은 TF 판단(`kind`)으로 전환확률을 정하고 조서는 GS
자신의 판단으로 정했다. GS 모형이므로 GS 판단이 맞다. 엔진을 조서에 맞췄고, 책 4-2
재현이 128.1781 로 오히려 가까워졌다 (책 128.1790).

## 3.5 수식 조서가 신용위험 처리 선택을 무시했다

사이드바에서 **GS 를 골라도 수식 조서는 언제나 TF 값**을 실었다.
`build_xlsx_formula` 가 `tm.model` 을 한 번도 읽지 않았기 때문이다. 결과 시트의
70% 트랜치는 ⑧(TF)에서, 30% 트랜치는 ⑮(TF)에서 그대로 끌어왔고, GS 값 하나가
참고용으로 옆에 놓여 있을 뿐이었다.

티사이언티픽 조건에서 벌어진 폭이다.

| | 앱 화면 (GS) | 수식 조서 (TF 고정) | 차이 |
|---|---|---|---|
| 전환사채 전체 | 116.4037 | 120.0576 | 3.65 |
| 매도청구권 | 12.5044 | 13.5173 | 1.01 |
| 전환권대가 | 39.3208 | 40.3336 | 1.01 |

값 조서는 계산이 끝난 숫자를 받아 적으므로 옳았다. **같은 평가에서 두 조서가
다른 답을 내던 상태**였다.

고친 방법은 이렇다.

- **⑮ 30% 트랜치에 GS 를 붙였다.** ⑪~⑭ 와 같은 순서(전환확률 → 위험조정할인율 →
  보유가치 → 금융상품가치)를 30% 트랜치 자신의 헤더와 전환가치로 다시 놓았다.
  사·아·자·차 네 블록이다. 만기에서는 TF 와 GS 가 같으므로 그 열은 공유한다.
- **가정 시트에 `신용위험 처리 (0 TF / 1 GS)` 를 넣었다.** 전환권 분류·매도청구권
  평가방법과 같은 방식이다.
- **결과 시트 1번에 `적용 · 70% 트랜치` 와 `적용 · 30% 트랜치` 두 행을 두었다.**
  아래 구성요소는 모두 이 두 행만 본다.

주계약과 부채요소는 **전환이 없어 지분 몫이 0** 이므로 혼합할인율이 신용할인율과
같아져 TF 와 GS 가 항상 일치한다. 8개 조건에서 차이가 1e-14 이하임을 확인했다.
모형 선택이 갈라지는 곳은 트랜치 둘뿐이라 거기서 한 번만 고르면 된다.

**대조 범위도 넓혔다.** 전에는 결과 시트 9개 셀만 봤다. 이제 회계처리 시트의
배분표 각 줄과 분개 차·대변 합계까지 본다. 배분과 분개가 같은 시트 안에서
어긋났던 적이 있어 이 둘은 반드시 함께 봐야 한다. 시나리오도 10개로 늘려
TF·GS × 자본·부채 × 유무가치·옵션차익을 모두 지나가게 했다.

이제 **10 시나리오 × 13 항목**이 소수점까지 일치한다. 다시 어긋나지 않도록
`tests/조서대조.py` 로 자동화했다.

## 3.6 경제적 성질을 따로 확인했다

수치 대조와 별개로, 결과가 **반드시 만족해야 하는 성질**을 점검했다.

- **단조성** — 변동성↑·주가↑ 는 전환사채 가치를 올리고, 할인율↑ 은 내리며,
  매도청구 한도↑ 는 매도청구권 가치를 올린다. 모두 성립.
- **항등식** — 전환권대가 = 100 − 부채요소 + 매도청구권. 상각표의
  기초 + 이자 − 지급 = 만기상환금액. 표면이자 유무·지급주기와 무관하게 성립.
- **수렴** — 노드 간격을 월·2주·주로 바꾸면 전환사채 가치가 120.06 · 120.48 ·
  120.34 로 **폭 0.43(0.36%)** 안에서 움직인다. 격자 모형의 통상적인 진동이다.
  UI 는 이 세 간격만 제공한다. 더 성긴 간격(분기·6개월)에서는 계약상 조기상환
  12회·매도청구 5회·리픽싱 8회가 노드에 다 실리지 못해 회수 자체가 달라진다.
- **후속측정** — 평가기준일을 발행 후 0·6·12·24·36·54·59개월로 옮겨도 배분
  항등식과 유효이자율이 모두 정상이다. 24개월 뒤 매도청구권이 0 이 되는 것은
  매도청구 기간이 24개월에 끝나기 때문이고, 59개월에 전환권이 0 이 되는 것은
  전환 종료가 59개월이기 때문이다. 둘 다 계약 조건대로다.
- **극단 입력** — 변동성 5%~150%, 주가 전환가액의 1/10~10배, 표면 > 보장,
  전환기간 1노드, 옵션 전부 없음 등 12가지에서 발산·예외 없음.
- **시나리오 JSON** — 저장·복원 후 결과가 같고, `k_method` 가 없는 옛 파일도
  기본값 0 으로 읽힌다.

## 3.7 수식 조서를 값 조서와 같은 수준으로 채웠다

세 가지가 빠져 있었다.

**방법 2(지분·부채 분리)가 없었다.** 값 조서에는 있고 수식 조서는 방법 1로
대체하고 있었다. ㉑~㉔ 네 시트를 새로 놓아 채웠다.

| 시트 | 하는 일 |
|---|---|
| ㉑ 방법2 지분보유 | 다음 두 칸의 지분 몫을 **무위험** 선도이자율로 할인 |
| ㉒ 방법2 부채보유 | 다음 두 칸의 부채 몫을 **위험** 선도이자율로 할인 |
| ㉓ 방법2 지분몫 | 행사하면 페이오프 × 구성비율, 아니면 ㉑ |
| ㉔ 방법2 부채몫 | 행사 판단은 ㉓ 과 같고, 나머지가 부채 몫 |

방법 1이 값 **하나**를 섞은 할인율로 할인한다면, 방법 2는 값을 **둘로 쪼개**
각각 제 이자율로 할인한다. 행사 판단은 쪼개기 전 합계로 한다. 결과 시트에
세 방법이 나란히 서고, 가정 시트의 평가방법 값이 어느 것을 적용할지 고른다.
티사이언티픽 조건에서 8.8101 로 엔진과 소수점까지 같다.

**이자율곡선 시트가 없었다.** 각 트리 11·12행의 선도이자율이 어디서 왔는지
추적할 길이 없었다. 값 조서와 같은 표(현물·선도·스프레드, 부트스트래핑)를 놓았다.

**상각표가 없었다.** 유효이자율만 역산 결과라 값으로 두고, 상각 내역은 살아 있는
수식으로 만들었다. 마지막 줄에서 기말 잔액이 만기상환금액과 맞는지 검산한다.
무이표·표면 3% 반기·표면 8% 분기 세 조건에서 회차별 네 열(기초·이자비용·지급이자·
기말)이 모두 엔진과 일치한다.

수식 조서는 이제 **31시트**다.

## 3.8 배선을 따로 훑다가 오류 셋을 더 찾았다

값이 맞는지만 보면 **기본값이 우연히 같아서 가려지는 오류**를 놓친다. 그래서
머리 행 수식이 가정 시트의 **어느 항목**을 참조하는지 이름 단위로 훑었다.
`tests/배선대조.py` 다.

**① 수식 조서의 매도청구 Flag 가 조기상환 주기를 참조했다.** 기본값이 둘 다
3개월이라 값 대조를 계속 통과했다. 매도청구 주기를 1개월로 두는 순간 행사 시점이
통째로 달라진다. 가정 시트에 `매도청구 주기 (스텝)` 을 따로 두어 끊었다.

**② 두 조서 모두 리픽싱 시점에 경과기간 오프셋을 넣지 않았다.** 다음 조정일은
평가기준일부터 **(주기 − 경과분)** 뒤인데, 두 조서는 그냥 주기의 배수마다 조정한다고
표시했다. 엔진은 처음부터 오프셋을 쓰고 있었으므로 조서만 틀렸다.

| 경과 | 엔진이 보는 조정 스텝 | 조서가 표시하던 것 |
|---|---|---|
| 0개월 | 7 · 14 · 21 · 28 | 7 · 14 · 21 · 28 |
| 3개월 | **4 · 11 · 18 · 25** | 7 · 14 · 21 · 28 |
| 6개월 | **1 · 8 · 15 · 22** | 7 · 14 · 21 · 28 |
| 12개월 | **2 · 9 · 16 · 23** | 7 · 14 · 21 · 28 |

경과기간이 조정 주기의 배수가 아닌 **모든 중간평가**에서 어긋났다. 가정 시트에
`첫 조정 스텝` 을 두고 두 조서를 엔진에 맞췄다.

**③ 값 조서가 행사 시점에서 경과기간을 빼지 않았다.** `stp` 가 계약상 개월을
그대로 스텝으로 바꾸고 있었다. 엔진과 수식 조서는 경과분을 뺀다. 트리 값 자체는
엔진에서 받아 적으므로 옳았지만, **Flag 행과 행사금액 행이 통째로 밀렸다.**

| 경과 | 첫 조기상환 청구 스텝 | 그 자리에 적힌 금액 | 옳은 금액 |
|---|---|---|---|
| 3개월 | 24 → **21** | 112.0789 | **110.6416** |
| 6개월 | 24 → **18** | 113.5425 | **110.6422** |
| 12개월 | 24 → **12** | 116.5397 | **110.6383** |
| 18개월 | 24 → **6** | 119.6518 | **110.6378** |

조서를 읽는 사람이 "이 노드에서 왜 상환 판단이 나왔지" 를 Flag 행으로 확인하려 하면
맞지 않는 표를 보게 된다. 최초 인식(경과 0)에서는 셋 다 드러나지 않는다.

## 3.9 모든 설정을 하나씩 흔들어 봤다

`tests/조서대조.py` 가 대표 시나리오를 깊게 본다면, 새로 만든
`tests/설정전수대조.py` 는 **설정을 하나씩 바꿔** 넓게 본다. 43가지 설정 변경을
지나가며 두 가지를 함께 확인한다.

1. **조서 값 == 엔진 값** — 배선이 맞는가
2. **결과가 기본값에서 움직였는가** — 그 설정이 실제로 결과를 바꾸는가

2번이 없으면 "아무도 참조하지 않는 설정"을 통과시켜 버린다. 결과가 안 움직여도
되는 설정(전자등록총액 — 100 기준 결과는 그대로다)은 이유를 목록에 적어 둔다.

**시트 배선도 정적으로 훑었다.** 수식 조서의 모든 수식을 읽어 어느 시트가 어느
시트를 참조하는지 그렸다. 아무도 참조하지 않는 트리 시트는 없고, 가정 시트 항목
중 어떤 수식도 쓰지 않는 것은 발행일·평가기준일·만기일 셋뿐이다. 이 셋은 기록용이고
계산에 쓰이는 정보는 경과기간·잔존기간·노드 수가 이미 담고 있다.

**참고 — 방법 1·2 는 TF·GS 선택과 무관하다.** 두 방법 모두 구성비율을 TF 트리의
지분·부채에서 가져오기 때문이다. 엔진과 조서가 같은 방식이라 어긋나지 않는다.
매도청구권을 유무가치비교법으로 재면 트랜치 두 개의 차이라 모형을 따라 움직인다.

## 3.10 검사가 스스로를 속이고 있던 두 가지

전수 대조를 돌리다 **검사 자체의 맹점** 둘을 찾았다. 조서 오류는 아니지만,
"검사를 통과했다" 는 말의 값어치를 깎는 것들이라 적어 둔다.

**① 검사 도구가 COMBIN 을 못 푼다.** `formulas` 라이브러리에 COMBIN 이 없다.
도달확률 시트가 COMBIN 을 쓰므로 그 시트를 참조하는 순간 오류가 번져 대조가
통째로 무의미해진다. 조서는 정상이다 — 엑셀은 COMBIN 을 계산한다.

문제는 **왜 지금까지 안 걸렸느냐** 다. 계산 시간을 줄이려고 노드를 10개로 줄여
검사했는데, 그러면 리픽싱 주기 7개월이 1스텝으로 뭉개져 **모든 열이 조정일**이
된다. 조정일이 아닌 시점을 어떻게 처리하느냐(경로가중치·확률가중평균·특정노드선택)를
묻는 수식이 아예 생기지 않으니 도달확률을 참조할 일도 없었다. 즉 **조정일 처리
로직이 한 번도 검증된 적이 없었다.** 검사가 통과한 이유가 "맞아서" 가 아니라
"그 경로를 안 지나가서" 였다.

COMBIN 을 상수로 바꿔 푸는 우회를 넣고 노드를 촘촘히 해 다시 재니 **오류가 나왔다.**

### 리픽싱 이월 계산이 한 스텝 뒤의 q 를 썼다

조정일이 아닌 시점에서는 직전 두 칸의 전환가액을 이월해 온다. 그 가중치가
위험중립가중치 q 인데, 수식 조서가 **그 열의 q** 를 쓰고 있었다. 엔진은
**직전 구간의 q** 를 쓴다. 열 i 의 q 는 구간 [i, i+1] 의 것이므로 i−1 에서 i 로
넘어올 때는 열 i−1 의 q 여야 한다. 앞서 할인율에서 고친 것과 같은 종류의 실수다.

도달확률 시트도 마찬가지였다. 열마다 제 q 를 써야 하는데 평탄 곡선에서 뽑은
q 하나를 모든 열에 쓰고 있었다.

| 조정일 처리 | 고치기 전 조서 | 엔진 | 고친 뒤 |
|---|---|---|---|
| 경로가중치 | 111.81460 | 111.81127 | **111.81127** |
| 확률가중평균 | 111.78896 | 111.78848 | **111.78848** |
| 특정노드선택 | 111.11008 | 111.11008 | 111.11008 |

**특정노드선택만 처음부터 맞았다.** 그 방식은 q 를 아예 쓰지 않기 때문이다.
이 사실이 원인을 정확히 가리켜 주었다.

고친 뒤 세 방식 모두 소수점 여섯 자리까지 일치한다.

**② 설정을 바꿔도 값이 안 움직이면 배선이 없는지 계약 때문인지 갈라야 한다.**
전수 대조가 "결과가 움직였는가" 를 함께 보게 한 이유다. 실제로 열한 건이 걸렸고,
파 보니 셋으로 나뉘었다.

- **격자가 뭉갠 것** — 노드 10개에서는 조기상환 주기 3개월과 6개월이 모두
  1스텝이라 같은 값이 나온다. 격자를 촘촘히 해 갈랐다. 리픽싱 상향 허용 여부도
  분기 노드·주기 7개월에서는 엔진조차 같은 값을 내므로, 주기를 12개월로 늘려
  1.49 만큼 갈리게 했다.
- **수학적으로 같아지는 것** — 조정 주기가 2스텝이면 사이 칸이 하나뿐이라
  세 근사가 정말 같다. 주기를 12개월로 늘려 4스텝을 만들어야 갈라진다.
  월 노드에서는 112.9391 · 112.8547 · 112.5261 로 갈린다.
- **계약 때문에 정상인 것** — 기본 조건은 **의무보유 25개월 > 매도청구 종료
  24개월**이라, 콜 행사기간 내내 전환이 막혀 있어 첫 행사일에 콜이 확정된다.
  뒤에 행사일이 몇 개든 값이 같은 것이 맞다. 의무보유를 풀면 주기에 따라
  112.1994 · 113.5336 · 113.5968 로 움직인다. 주기 배선을 보려면 의무보유를
  풀고 재야 한다.

전수 대조 케이스를 이에 맞춰 다시 짰다. 격자별로 기준선을 따로 잡아, 촘촘한
격자에서 도는 케이스도 "정말 움직였는가" 를 확인한다.

## 3.11 머리 행에 날짜를 넣고 스텝에 연결했다

**머리 1행 Date 가 비어 있었다.** 평가기준일 + 스텝 × Δt 로 각 열의 날짜를 넣어,
계약상 개시일과 트리의 열을 눈으로 대조할 수 있게 했다. 스텝 12에서 Flag(전환)이
안 켜지면 개월 환산을 잘못한 것이다. 가정 시트의 날짜 세 개도 문자열이 아니라
진짜 날짜로 바꿨다.

날짜 환산은 **365일**로 한다. 잔존기간 T 가 (만기일−평가기준일)/365 이므로
365.25 를 쓰면 만기 열이 하루 어긋난다.

**머리 행이 스텝 번호를 상수로 박아 쓰고 있었다.**

    고치기 전   =IF(AND(0>=가정!$C$23, 0<=가정!$C$24),1,0)
    고친 뒤     =IF(AND(C$2>=가정!$C$23, C$2<=가정!$C$24),1,0)

열마다 다른 상수를 품고 있어 "살아 있는 수식" 이라 하기 어려웠다. 이제 1행과
3~10행이 모두 그 열의 2행을 참조하고, 2행 자신도 직전 열 + 1 이라 맨 앞 열의
0 하나에서 모든 열이 정해진다. ⑯ 부채요소도 같은 구조로 맞췄다.

## 3.12 상태확장을 고르면 두 조서가 갈렸다

**앱의 기본 설정에서 값 조서와 수식 조서가 다른 답을 냈다.**

리픽싱이 있으면 조정일마다 전환가액이 갈라진다. 위에서 온 경로와 아래서 온
경로가 같은 칸에서 만나도 서로 다른 전환가액을 들고 온다. **상태확장**은 이를
근사하지 않고 갈라진 경로를 전부 들고 간다 — 가장 정확하지만 격자가 재결합하지
않아 **엑셀 트리 한 장으로 표현할 수 없다.**

그래서 수식 조서는 경로가중치 근사로 다시 계산하는데, **그 사실을 아무 데도
적지 않았다.** 아이씨티케이 조건(리픽싱 7개월·풋·콜·GS)에서 이만큼 갈렸다.

| | 전체 (GS) | 매도청구권 |
|---|---|---|
| 상태확장 — 값 조서 | 134.9296 | 15.4949 |
| 경로가중치 — 수식 조서 | 128.3875 | 13.6498 |
| **차이** | **6.5421** | **1.8450** |

**왜 안 걸렸나.** 조서 대조 검사가 모두 `carry=1` 을 강제하고 있었다. 계산
시간을 줄이려던 설정이 정확히 문제 지점을 가렸다. 3.10 의 COMBIN 과 같은
종류다 — 검사가 통과한 이유가 "맞아서" 가 아니라 "그 경로를 안 지나가서" 였다.

**조서가 스스로 말하게 했다.** 결과 시트 검산에 `앱 계산값 · 상태확장 격자`
한 줄을 넣었다. 상태확장이면 앱의 정확값을 주황색으로 싣고 조서 값과 다르면
"★ 조서는 근사값" 이라고 판정한다. 그 아래 붉은 글씨로 왜 옮길 수 없는지와
어느 숫자가 정확한지 적는다. 다른 방법이면 "해당 없음" 으로 비운다.

`tests/배선대조.py` 에 네 가지 조정일 처리를 모두 지나가는 검사를 넣어 다시
숨지 못하게 했다.

**실무 지침** — 수식 조서를 제출한다면 처음부터 근사 방법을 고르는 것이 맞다.
상태확장으로 평가한다면 값 조서를 정본으로 삼고 수식 조서는 산식 검증용으로만
쓰며, 검산 줄의 차이를 조서에 적는다. 세 근사법의 차이도 작지 않다 —
같은 사채에서 경로가중치 128.39, 확률가중평균 128.23, 특정노드선택 125.84 다.

## 4. 남겨둔 것

### 4.1 제3자 콜옵션 접근법 1과 2의 선택

책도 정하지 않는다 (위 2.1 참고). 어느 쪽을 골랐는지 조서에 남기고 후속 결산에
일관되게 적용하는 것이 4.6이 요구하는 전부다. 세 방법 모두 값·수식 두 조서에서
계산되므로, 조서를 열면 고르지 않은 방법의 값도 함께 보인다.

---

## 5. 새 방법론 — 이번 범위 밖

`docs/책예제_대조표.md` 에 정리해둔 IPO 리픽싱(3.8.1)·Backsolve(3.4.2)·
BDT(3.2.2.4)·수익성 지표 조건(3.8.4)·지분가치배분법(3.9.2)은 기존 격자 밖의
새 기능이라 넣지 않았다.
