streamlit>=1.36
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
openpyxl>=3.1
# pykrx 는 스트림릿 클라우드에서 설치가 자주 실패해 뺐다
# yfinance 0.2.58 부터 curl_cffi 를 요구한다. 그 휠은 파이썬 3.10 이상에만
# 있어서 3.9 환경에서는 소스 빌드로 넘어가 설치가 통째로 실패한다.
# 그래서 파이썬 판으로 갈라 둔다. 3.10 이상이면 최신을 쓴다 — 옛 판은 야후가
# API 를 바꿀 때마다 빈 응답을 받아 "자료 없음" 으로 조용히 실패한다.
yfinance>=0.2.60; python_version >= "3.10"
yfinance>=0.2.40,<0.2.58; python_version < "3.10"
xlrd>=2.0
